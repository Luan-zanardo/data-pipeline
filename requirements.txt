# Documentação
mkdocs-material

# Geração de massa Mock (Etapa 2)
faker

# Ingestão da camada Landing (execução local fora do Airflow)
psycopg2-binary==2.9.9

# Processamento Spark (Camadas Bronze/Silver)
pyspark==3.5.0
delta-spark==3.2.0
apache-airflow-providers-apache-spark

# Escrita no object storage S3-compatible / MinIO (s3fs já puxa o fsspec)
s3fs==2024.6.1
