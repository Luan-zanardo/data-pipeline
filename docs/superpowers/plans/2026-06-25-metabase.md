# Metabase (Etapa 6) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar Metabase self-host ao stack Docker, com data source (Postgres de destino) e um dashboard de 6 cards (4 KPIs + 2 métricas) provisionados automaticamente via API.

**Architecture:** Três serviços novos no `docker-compose.yml` — `metabase-db` (Postgres do app), `metabase` (app em `:3000`) e `metabase-init` (job único que provisiona admin, data source e dashboard via REST API usando `curl` + `jq`). A lógica de provisionamento fica num script versionado `scripts/metabase_provision.sh`, idempotente.

**Tech Stack:** Docker Compose, Metabase REST API, shell POSIX (`sh`), `curl`, `jq`, Postgres 16.

**Spec:** `docs/superpowers/specs/2026-06-25-metabase-design.md`

---

## Notas de ambiente (ler antes de começar)

- **Sem framework de testes:** o projeto não tem pytest/`tests/`. A "verificação" de cada tarefa usa lint de YAML (`docker compose config -q`), checagem de sintaxe shell (`sh -n`) e, na Task 7, um teste de integração real.
- **Docker Desktop:** os passos de integração (Task 7) exigem o Docker Desktop **Running**. Os passos de lint/sintaxe (Tasks 1–6) rodam sem subir containers.
- **Shell:** o ambiente de desenvolvimento é Windows/PowerShell. Comandos `docker compose ...` funcionam no PowerShell. Para `sh -n`, use `docker run --rm -v ...` (mostrado nas tasks) caso não haja `sh` nativo.
- **Decisão técnica (robustez):** `metabase-init` depende de `metabase` com `condition: service_started` (não `service_healthy`). O próprio script aguarda `/api/health` num laço `until`. Isso evita travar caso a imagem do Metabase reporte o healthcheck de forma diferente entre versões. O healthcheck do `metabase` continua definido (para `docker compose ps`), mas não bloqueia o init.
- **Metabase API (versão atual):** cards são adicionados ao dashboard via `PUT /api/dashboard/:id` com um array `dashcards` (cada novo dashcard usa `id` negativo temporário). O antigo `POST /api/dashboard/:id/cards` foi descontinuado.

---

## File Structure

- **Modify** `.env.example` — adicionar `MB_ADMIN_EMAIL` e `MB_ADMIN_PASSWORD`.
- **Modify** `docker-compose.yml` — adicionar serviços `metabase-db`, `metabase`, `metabase-init` e o volume `metabase-db-data`.
- **Create** `scripts/metabase_provision.sh` — script idempotente de provisionamento (setup + data source + cards + dashboard).
- **Modify** `docs/metabase.md` — refletir o provisionamento automático.

---

## Task 1: Adicionar credenciais de admin ao `.env.example`

**Files:**
- Modify: `.env.example` (final do arquivo, após o bloco `MB_DB_*`)

- [ ] **Step 1: Editar `.env.example`**

Localize o bloco que termina em `MB_DB_PASSWORD=metabase` (linha ~50) e, logo abaixo dele (antes do bloco `AIRFLOW_UID`), insira:

```dotenv

# ---- Admin inicial do Metabase (provisionado automaticamente pelo init) -----
# O metabase-init cria este usuario admin no primeiro acesso e ja conecta o
# data source (DEST_DB_*) e o dashboard. A senha precisa ter complexidade minima.
MB_ADMIN_EMAIL=admin@example.com
MB_ADMIN_PASSWORD=metabaseadmin1
```

- [ ] **Step 2: Verificar que o arquivo continua um dotenv válido**

Run (PowerShell): `Select-String -Path .env.example -Pattern '^MB_ADMIN_(EMAIL|PASSWORD)='`
Expected: duas linhas correspondentes (`MB_ADMIN_EMAIL=...` e `MB_ADMIN_PASSWORD=...`).

- [ ] **Step 3: Commit**

```bash
git add .env.example
git commit -m "feat(metabase): adiciona credenciais de admin no .env.example"
```

---

## Task 2: Adicionar serviço `metabase-db` e o volume

**Files:**
- Modify: `docker-compose.yml` (bloco `services:` e bloco `volumes:`)

- [ ] **Step 1: Adicionar o serviço `metabase-db`**

No `docker-compose.yml`, dentro de `services:`, após o serviço `airflow-scheduler` (antes da linha `volumes:`), adicione:

```yaml
  # ---- Etapa 6 — Dataviz (Metabase) ----------------------------------------
  metabase-db:
    image: postgres:16
    environment:
      POSTGRES_DB: ${MB_DB_NAME:-metabase}
      POSTGRES_USER: ${MB_DB_USER:-metabase}
      POSTGRES_PASSWORD: ${MB_DB_PASSWORD:-metabase}
    volumes:
      - metabase-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${MB_DB_USER:-metabase}"]
      interval: 10s
      retries: 5
      start_period: 10s
    restart: unless-stopped
```

- [ ] **Step 2: Adicionar o volume**

No bloco `volumes:` ao final do arquivo, adicione a entrada `metabase-db-data`:

```yaml
volumes:
  airflow-metadata-data:
  minio-data:
  metabase-db-data:
```

- [ ] **Step 3: Validar o compose**

Run: `docker compose config -q`
Expected: sem saída e exit code 0 (YAML válido; variáveis com default resolvem mesmo sem `.env`).

- [ ] **Step 4: Commit**

```bash
git add docker-compose.yml
git commit -m "feat(metabase): adiciona Postgres dedicado do app (metabase-db)"
```

---

## Task 3: Adicionar serviço `metabase`

**Files:**
- Modify: `docker-compose.yml` (após o serviço `metabase-db`)

- [ ] **Step 1: Adicionar o serviço `metabase`**

Logo após o serviço `metabase-db`, adicione:

```yaml
  metabase:
    image: metabase/metabase:latest
    ports:
      - "3000:3000"
    environment:
      MB_DB_TYPE: postgres
      MB_DB_DBNAME: ${MB_DB_NAME:-metabase}
      MB_DB_USER: ${MB_DB_USER:-metabase}
      MB_DB_PASS: ${MB_DB_PASSWORD:-metabase}
      MB_DB_HOST: metabase-db
      MB_DB_PORT: 5432
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:3000/api/health"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s
    restart: unless-stopped
    depends_on:
      metabase-db:
        condition: service_healthy
```

- [ ] **Step 2: Validar o compose**

Run: `docker compose config -q`
Expected: sem saída e exit code 0.

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat(metabase): adiciona o servico metabase (app em :3000)"
```

---

## Task 4: Criar o script de provisionamento

**Files:**
- Create: `scripts/metabase_provision.sh`

- [ ] **Step 1: Criar `scripts/metabase_provision.sh` com o conteúdo completo**

```sh
#!/bin/sh
# =============================================================================
# Provisionamento automatico do Metabase (Etapa 6).
# Idempotente: pode rodar varias vezes sem duplicar nada.
#   1. aguarda /api/health
#   2. setup (admin + data source) se a instancia for nova (setup-token)
#   3. login -> sessao
#   4. descobre o id do data source "Gold (destino)"
#   5. se o dashboard ainda nao existir, cria 6 cards e o dashboard
# Requisitos no container: curl, jq. Variaveis: MB_URL, MB_ADMIN_EMAIL,
# MB_ADMIN_PASSWORD, DEST_DB_HOST/PORT/NAME/USER/PASSWORD/SSLMODE.
# =============================================================================
set -eu

MB_URL="${MB_URL:-http://metabase:3000}"
SITE_NAME="Data Pipeline"
DB_DISPLAY_NAME="Gold (destino)"
DASHBOARD_NAME="Pipeline — Vendas"

log() { echo "[metabase-init] $*"; }

# 1. Aguarda o Metabase ficar de pe ----------------------------------------
log "Aguardando o Metabase em ${MB_URL} ..."
until curl -sf "${MB_URL}/api/health" >/dev/null 2>&1; do
  log "ainda nao saudavel; aguardando 3s..."
  sleep 3
done
log "Metabase saudavel."

# Deriva o flag ssl a partir do sslmode do destino
case "${DEST_DB_SSLMODE:-require}" in
  disable|allow|prefer) DEST_SSL=false ;;
  *)                    DEST_SSL=true  ;;
esac

# 2. Setup (idempotente) ----------------------------------------------------
SETUP_TOKEN=$(curl -sf "${MB_URL}/api/session/properties" | jq -r '.["setup-token"] // empty')

if [ -n "${SETUP_TOKEN}" ]; then
  log "Instancia nova: executando setup (admin + data source)..."
  SETUP_PAYLOAD=$(jq -n \
    --arg token "$SETUP_TOKEN" \
    --arg email "$MB_ADMIN_EMAIL" \
    --arg password "$MB_ADMIN_PASSWORD" \
    --arg site "$SITE_NAME" \
    --arg dbdisp "$DB_DISPLAY_NAME" \
    --arg host "$DEST_DB_HOST" \
    --arg port "$DEST_DB_PORT" \
    --arg dbname "$DEST_DB_NAME" \
    --arg user "$DEST_DB_USER" \
    --arg pass "$DEST_DB_PASSWORD" \
    --argjson ssl "$DEST_SSL" \
    '{
      token: $token,
      user: {email: $email, password: $password,
             first_name: "Admin", last_name: "Pipeline", site_name: $site},
      prefs: {site_name: $site, allow_tracking: false},
      database: {
        engine: "postgres",
        name: $dbdisp,
        details: {host: $host, port: ($port|tonumber), dbname: $dbname,
                  user: $user, password: $pass, ssl: $ssl}
      }
    }')
  curl -sf -X POST "${MB_URL}/api/setup" \
    -H "Content-Type: application/json" \
    -d "$SETUP_PAYLOAD" >/dev/null
  log "Setup concluido."
else
  log "Instancia ja configurada: pulando setup."
fi

# 3. Login ------------------------------------------------------------------
log "Autenticando como admin..."
SESSION=$(curl -sf -X POST "${MB_URL}/api/session" \
  -H "Content-Type: application/json" \
  -d "$(jq -n --arg u "$MB_ADMIN_EMAIL" --arg p "$MB_ADMIN_PASSWORD" \
        '{username:$u, password:$p}')" \
  | jq -r '.id // empty')

if [ -z "$SESSION" ]; then
  log "ERRO: falha ao autenticar."
  exit 1
fi
AUTH="X-Metabase-Session: ${SESSION}"

# 4. Descobre o id do data source ------------------------------------------
DB_ID=$(curl -sf -H "$AUTH" "${MB_URL}/api/database" \
  | jq -r --arg name "$DB_DISPLAY_NAME" \
      '(.data // .) | map(select(.name == $name)) | .[0].id // empty')

if [ -z "$DB_ID" ]; then
  log "ERRO: data source '${DB_DISPLAY_NAME}' nao encontrado."
  exit 1
fi
log "Data source id = ${DB_ID}."

# 5. Idempotencia: dashboard ja existe? ------------------------------------
EXISTING=$(curl -sf -H "$AUTH" "${MB_URL}/api/dashboard" \
  | jq -r --arg name "$DASHBOARD_NAME" \
      '(.data // .) | map(select(.name == $name)) | .[0].id // empty')

if [ -n "$EXISTING" ]; then
  log "Dashboard '${DASHBOARD_NAME}' ja existe (id ${EXISTING}). Nada a fazer."
  exit 0
fi

# Cria um card nativo e ecoa o id retornado.
# $1=nome  $2=display  $3=sql
create_card() {
  curl -sf -X POST "${MB_URL}/api/card" \
    -H "$AUTH" -H "Content-Type: application/json" \
    -d "$(jq -n \
          --arg name "$1" --arg display "$2" --arg sql "$3" --argjson db "$DB_ID" \
          '{
            name: $name,
            display: $display,
            visualization_settings: {},
            dataset_query: {type: "native", native: {query: $sql}, database: $db}
          }')" \
    | jq -r '.id'
}

log "Criando cards..."
KPI1=$(create_card "Faturamento total" "scalar" \
  "SELECT SUM(f.quantidade * f.preco) AS faturamento_total FROM fato_vendas f;")
KPI2=$(create_card "Total de pedidos" "scalar" \
  "SELECT COUNT(DISTINCT f.pedido_id) AS total_pedidos FROM fato_vendas f;")
KPI3=$(create_card "Ticket medio por pedido" "scalar" \
  "SELECT SUM(f.quantidade * f.preco) / NULLIF(COUNT(DISTINCT f.pedido_id), 0) AS ticket_medio FROM fato_vendas f;")
KPI4=$(create_card "Itens vendidos" "scalar" \
  "SELECT SUM(f.quantidade) AS itens_vendidos FROM fato_vendas f;")
MET1=$(create_card "Faturamento por mes" "line" \
  "SELECT date_trunc('month', d.data) AS mes, SUM(f.quantidade * f.preco) AS faturamento FROM fato_vendas f JOIN dim_data d ON d.data = date(f.data_pedido) GROUP BY 1 ORDER BY 1;")
MET2=$(create_card "Top 10 produtos por faturamento" "bar" \
  "SELECT p.nome AS produto, SUM(f.quantidade * f.preco) AS faturamento FROM fato_vendas f JOIN dim_produto p ON p.id_produto = f.produto_id AND p.is_current = true GROUP BY 1 ORDER BY 2 DESC LIMIT 10;")

log "Criando dashboard '${DASHBOARD_NAME}'..."
DASH_ID=$(curl -sf -X POST "${MB_URL}/api/dashboard" \
  -H "$AUTH" -H "Content-Type: application/json" \
  -d "$(jq -n --arg name "$DASHBOARD_NAME" '{name:$name}')" \
  | jq -r '.id')

log "Adicionando cards ao dashboard..."
DASHCARDS=$(jq -n \
  --argjson k1 "$KPI1" --argjson k2 "$KPI2" --argjson k3 "$KPI3" --argjson k4 "$KPI4" \
  --argjson m1 "$MET1" --argjson m2 "$MET2" \
  '[
    {id:-1, card_id:$k1, row:0, col:0,  size_x:6,  size_y:3},
    {id:-2, card_id:$k2, row:0, col:6,  size_x:6,  size_y:3},
    {id:-3, card_id:$k3, row:0, col:12, size_x:6,  size_y:3},
    {id:-4, card_id:$k4, row:0, col:18, size_x:6,  size_y:3},
    {id:-5, card_id:$m1, row:3, col:0,  size_x:12, size_y:6},
    {id:-6, card_id:$m2, row:3, col:12, size_x:12, size_y:6}
  ]')

curl -sf -X PUT "${MB_URL}/api/dashboard/${DASH_ID}" \
  -H "$AUTH" -H "Content-Type: application/json" \
  -d "$(jq -n --argjson dc "$DASHCARDS" '{dashcards: $dc}')" >/dev/null

log "Dashboard provisionado com sucesso (id ${DASH_ID})."
```

- [ ] **Step 2: Checar a sintaxe do script**

Run (PowerShell, usando um container para ter `sh`):
`docker run --rm -v ${PWD}/scripts:/s alpine:3.20 sh -n /s/metabase_provision.sh`
Expected: sem saída e exit code 0 (sintaxe POSIX válida).

- [ ] **Step 3: Commit**

```bash
git add scripts/metabase_provision.sh
git commit -m "feat(metabase): script idempotente de provisionamento (data source + dashboard)"
```

---

## Task 5: Adicionar serviço `metabase-init`

**Files:**
- Modify: `docker-compose.yml` (após o serviço `metabase`)

- [ ] **Step 1: Adicionar o serviço `metabase-init`**

Logo após o serviço `metabase`, adicione:

```yaml
  metabase-init:
    image: alpine:3.20
    depends_on:
      metabase:
        condition: service_started
    environment:
      MB_URL: http://metabase:3000
      MB_ADMIN_EMAIL: ${MB_ADMIN_EMAIL:-admin@example.com}
      MB_ADMIN_PASSWORD: ${MB_ADMIN_PASSWORD:-metabaseadmin1}
      DEST_DB_HOST: ${DEST_DB_HOST}
      DEST_DB_PORT: ${DEST_DB_PORT:-5432}
      DEST_DB_NAME: ${DEST_DB_NAME}
      DEST_DB_USER: ${DEST_DB_USER}
      DEST_DB_PASSWORD: ${DEST_DB_PASSWORD}
      DEST_DB_SSLMODE: ${DEST_DB_SSLMODE:-require}
    volumes:
      - ./scripts:/scripts:ro
    entrypoint:
      - /bin/sh
      - -c
      - |
        apk add --no-cache curl jq >/dev/null
        sh /scripts/metabase_provision.sh
    restart: on-failure
```

- [ ] **Step 2: Validar o compose**

Run: `docker compose config -q`
Expected: sem saída e exit code 0. (Se `DEST_DB_*` não estiver no ambiente, defina um `.env` a partir do `.env.example` antes — `docker compose` lê o `.env` automaticamente.)

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat(metabase): adiciona metabase-init (provisiona data source e dashboard)"
```

---

## Task 6: Atualizar a documentação

**Files:**
- Modify: `docs/metabase.md`

- [ ] **Step 1: Atualizar a lista de componentes**

Em `docs/metabase.md`, na seção "## Componentes no Docker", substitua o trecho que descreve "dois serviços" por três. Localize:

```markdown
O `docker-compose.yml` adiciona dois serviços para a Etapa 6:

- **`metabase`** — a aplicação Metabase, exposta em
  <http://localhost:3000>.
- **`metabase-db`** — um **Postgres dedicado** que guarda a configuração do
  próprio Metabase (dashboards, perguntas, usuários). Não confundir com o
  banco de **destino** (os dados Gold), que é externo e conectado como
  *data source*.
```

e troque por:

```markdown
O `docker-compose.yml` adiciona três serviços para a Etapa 6:

- **`metabase`** — a aplicação Metabase, exposta em
  <http://localhost:3000>.
- **`metabase-db`** — um **Postgres dedicado** que guarda a configuração do
  próprio Metabase (dashboards, perguntas, usuários). Não confundir com o
  banco de **destino** (os dados Gold), que é externo e conectado como
  *data source*.
- **`metabase-init`** — um job de execução única que, no primeiro `up`, cria o
  usuário admin, conecta o **data source** (Postgres de destino) e monta o
  **dashboard "Pipeline — Vendas"** (4 KPIs + 2 métricas) via API do Metabase.
  É idempotente: re-execuções não duplicam nada.
```

- [ ] **Step 2: Substituir a seção de conexão manual pela automática**

Localize a seção inteira "## Conectando ao banco de destino (data source)" (do título até antes de "## Montando os dashboards") e substitua-a por:

```markdown
## Conexão e dashboard automáticos (metabase-init)

O serviço `metabase-init` provisiona tudo automaticamente no primeiro `up`,
usando as variáveis do `.env`:

- cria o **usuário admin** (`MB_ADMIN_EMAIL` / `MB_ADMIN_PASSWORD`);
- conecta o **data source** "Gold (destino)" com os valores de `DEST_DB_*`
  (o `ssl` é derivado de `DEST_DB_SSLMODE`);
- cria o dashboard **"Pipeline — Vendas"** com 4 KPIs (faturamento total, total
  de pedidos, ticket médio, itens vendidos) e 2 métricas (faturamento por mês e
  top 10 produtos por faturamento).

Acompanhe o provisionamento com:

​```bash
docker compose logs -f metabase-init
​```

!!! note "Pré-requisito de dados"
    O destino precisa estar populado para os cards exibirem dados. Rode antes o
    `src/spark/gold_to_postgres.py` (ver [Gold](gold.md)). Os cards são criados
    de qualquer forma; só ficam vazios até a Gold ser virtualizada.

### Reconfigurar manualmente (se necessário)

Para refazer o provisionamento do zero, derrube os volumes e suba de novo:

​```bash
docker compose down -v
docker compose up -d
​```

Ou ajuste o data source / dashboards diretamente pela UI em
**Admin settings → Databases** e na tela de dashboards.
```

> Nota para quem aplica o plano: nos três blocos de código acima, o caractere
> antes de cada ``` é um zero-width (`​`) só para escapar a cerca dentro deste
> plano. Ao escrever no `docs/metabase.md`, use crases normais ``` ``` ``` sem
> esse caractere.

- [ ] **Step 3: Validar o build da documentação (se o mkdocs estiver instalado)**

Run (PowerShell): `mkdocs build --strict`
Expected: build sem erros. Se o `mkdocs` não estiver instalado localmente, pule este passo (a doc é Markdown puro; confira visualmente que os blocos fecham corretamente).

- [ ] **Step 4: Commit**

```bash
git add docs/metabase.md
git commit -m "docs(metabase): descreve provisionamento automatico (init)"
```

---

## Task 7: Verificação de integração (end-to-end)

**Pré-requisitos:** Docker Desktop **Running**; `.env` criado a partir do
`.env.example` com `DEST_DB_*` preenchido e apontando para um Postgres de
destino acessível (idealmente já populado pelo `gold_to_postgres.py`).

- [ ] **Step 1: Garantir o `.env`**

Run (PowerShell): `if (-not (Test-Path .env)) { Copy-Item .env.example .env }`
Depois edite o `.env` e preencha `DEST_DB_HOST/NAME/USER/PASSWORD`.
Expected: arquivo `.env` presente e preenchido.

- [ ] **Step 2: Subir os serviços do Metabase**

Run: `docker compose up -d metabase-db metabase metabase-init`
Expected: os três containers criados sem erro de configuração.

- [ ] **Step 3: Acompanhar o init até concluir**

Run: `docker compose logs -f metabase-init`
Expected: termina com `Dashboard provisionado com sucesso (id N).` (primeira
execução) e o container sai com código 0. Encerre o `-f` com Ctrl+C após a
mensagem.

- [ ] **Step 4: Conferir a saúde do app**

Run: `docker compose ps metabase metabase-db metabase-init`
Expected: `metabase` `running`/`healthy`; `metabase-init` `exited (0)`.

- [ ] **Step 5: Conferir o data source e o dashboard via API**

Run (PowerShell):
```powershell
$body = @{ username = "admin@example.com"; password = "metabaseadmin1" } | ConvertTo-Json
$sess = (Invoke-RestMethod -Method Post -Uri "http://localhost:3000/api/session" -ContentType "application/json" -Body $body).id
$h = @{ "X-Metabase-Session" = $sess }
(Invoke-RestMethod -Uri "http://localhost:3000/api/database" -Headers $h).data.name
(Invoke-RestMethod -Uri "http://localhost:3000/api/dashboard" -Headers $h).name
```
Expected: a lista de databases inclui **"Gold (destino)"** e a lista de
dashboards inclui **"Pipeline — Vendas"**. (Ajuste usuário/senha se você os
alterou no `.env`.)

- [ ] **Step 6: Conferir o dashboard no navegador**

Abra <http://localhost:3000> (logado como admin) e verifique o dashboard
**"Pipeline — Vendas"** com os 6 cards. Com a Gold populada, os KPIs e gráficos
exibem dados.

- [ ] **Step 7: Verificar a idempotência**

Run: `docker compose up -d metabase-init`
Run: `docker compose logs --tail=20 metabase-init`
Expected: termina com `Dashboard 'Pipeline — Vendas' ja existe (id N). Nada a fazer.` — nenhum card/dashboard duplicado.

- [ ] **Step 8: Commit final (caso reste algo a registrar)**

Não há mudança de código nesta task; apenas validação. Se você corrigiu algum
ajuste durante a verificação, commit normalmente.

---

## Notas finais

- **Idempotência:** setup gated pelo `setup-token`; cards/dashboard gated pela
  existência do dashboard por nome.
- **Segredos:** o `.env` real não é commitado (já está no `.gitignore`). O
  `metabase-init` recebe os segredos por variável de ambiente, não os grava em
  arquivo.
- **Divergência de schema conhecida:** o SQL dos cards segue o schema **real**
  produzido por `src/spark/silver_to_gold.py` (ver a seção correspondente no
  spec), não o `docs/modelo_dimensional.md` idealizado.
